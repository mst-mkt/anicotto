import { Loader2 } from 'lucide-react'

export const Loading = () => (
  <div className="flex items-center justify-center p-16">
    <Loader2 className="animate-spin text-anicotto-accent" size={32} />
  </div>
)
